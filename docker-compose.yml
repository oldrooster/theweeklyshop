services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - theweeklyshop-data:/app/data
    env_file:
      - .env.local
    restart: unless-stopped

volumes:
  theweeklyshop-data:
